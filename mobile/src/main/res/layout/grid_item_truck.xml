<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_height="wrap_content"
    android:layout_width="wrap_content"
    android:orientation="vertical">

    <ImageView
        android:id="@+id/truck_image"
        android:layout_height="100dp"
        android:layout_width="100dp"
        android:layout_margin="10dp"/>

    <ImageView
        android:id="@+id/truck_serving_indicator"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/truck_serving_indicator"
        android:layout_alignBottom="@id/truck_image"
        android:layout_alignLeft="@id/truck_image"
        android:visibility="invisible"/>

    <TextView
        android:id="@+id/truck_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:gravity="center"
        android:textSize="16sp"
        tools:text="PizzaGuy"
        android:layout_below="@id/truck_image"
        android:layout_centerHorizontal="true" />

</RelativeLayout>